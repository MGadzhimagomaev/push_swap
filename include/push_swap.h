#ifndef PUSH_SWAP_H
# define PUSH_SWAP_H

#include "../libft/libft.h"

typedef struct s_node
{
	int		val;
	int		index;
	struct s_node	*next;
} t_node;
//define instructions

//define algorithm functions

#endif